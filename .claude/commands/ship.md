Review the current git diff and staged changes, then commit and push to main.

Steps:
1. Run `git status` and `git diff` to understand what changed
2. Stage only relevant source files (never .env or secrets)
3. Write a concise commit message that describes what changed and why
4. Commit and push to origin main

Keep the commit message short and lowercase (e.g. "add nav link to home page"). No need to ask for confirmation — just ship it.
